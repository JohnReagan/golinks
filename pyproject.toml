[tool.poetry]
name = "go-links"
version = "0.1.0"
description = ""
authors = ["John Reagan"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
